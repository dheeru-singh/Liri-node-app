{
  "name": "13-stu_inquirer-users",
  "version": "1.0.0",
  "description": "In this activity, you will build a simple command line application that accepts user input and writes it to a `.json` file. The application will ask the user for their first and last name, \"Ada Lovelace\", for example, and generate a file in this format: `adalovelace.json`.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.19.0",
    "bandsintown": "^1.0.1",
    "colors": "^1.3.3",
    "dotenv": "^8.0.0",
    "inquirer": "^6.5.0",
    "moment": "^2.24.0",
    "node-spotify-api": "^1.1.1"
  }
}
